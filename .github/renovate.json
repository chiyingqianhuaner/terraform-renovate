{
  "extends": ["config:base", ":dependencyDashboard"],
  "schedule": ["before 1am"],
  "includePaths": [
    "helm/gke/business-dev/**",
    "helm/gke/business-prod/**",
    "atlantis.yaml"
  ],
  "packageRules": [
    {
      "matchPaths": ["helm/gke/business-dev/**"],
      "updateTypes": ["major","patch"],
      "enabled": false
    },
    {
      "matchPaths": ["helm/gke/business-dev/**"],
      "matchDepTypes": ["module"],
      "rangeStrategy": "bump",
      "prTitle": "{{{groupName}}} Update {{{depName}}} from {{{currentValue}}} to {{{newValue}}}",
      "groupName": "business-dev-01"
    },
    {
      "matchPaths": ["helm/gke/business-prod/**"],
      "separateMajorMinor": true,
      "separateMultipleMajor": true,
      "prTitle": "{{{groupName}}} Update {{{depName}}} from {{{currentValue}}} to {{{newValue}}}",
      "groupName": "business-prod-01"
    }
  ],
  "regexManagers": [
    {
      "fileMatch": ["^atlantis\\.yaml$"],
      "matchStrings": [
        "terraform_version:\\s*v?(?<currentValue>[\\d\\.]+)"
      ],
      "extractVersionTemplate": "v(?<version>.*)$",
      "depNameTemplate": "hashicorp/terraform",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver"
    }
  ]
}
